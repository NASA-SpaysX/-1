# -*- coding: utf-8 -*-
"""
Тесты для простых заданий на print().
Если тест "падает", в сообщении будет подробный комментарий с подсказкой.

Ожидается, что ученики пишут решения в файле tasks.py
и реализуют соответствующие функции (см. список ниже).

Список функций, которые ДОЛЖНЫ быть в tasks.py:

1) print_name_age(name: str, age: int) -> None
   Печатает: "Имя: <name>, Возраст: <age>"

2) print_greeting() -> None
   Печатает: "Привет!Как твои дела?"

3) print_poem() -> None
   Печатает три строки (каждая с переводом строки):
     Береза под моим окном
     Принакрылась снегом.
     Точно серебром.

4) print_quote() -> None
   Печатает: Он сказал: "Учить Python - это весело!"

5) print_numbers_with_space() -> None
   Печатает: "1 2 3 4 5"

6) print_hello_exclamations() -> None
   Печатает три строки:
     Hello!
     Hello!
     Hello!

7) print_table_squares() -> None
   Печатает три строки с табуляцией между числом и квадратом:
     1\t1
     2\t4
     3\t9

8) print_sum_with_text() -> None
   Печатает: "5 + 10 = 15"

9) print_three_numbers_with_commas() -> None
   Печатает: "1, 2, 3"

ПОДСКАЗКА: Чтобы пройти тесты, просто повторите строки вывода ТОЧНО как указано выше.
"""
